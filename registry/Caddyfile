registry.JKTLUG.id {
    reverse_proxy registry:5000

    basicauth {
        # Generate your own hash with:
        #   docker run --rm caddy:2-alpine caddy hash-password
        # Then replace the line below.
        jktlug $2a$14$Zkx19XLiW6VYouLHR5NmfOFU0z2GTNmpkT/5qqR7hx4IjWJPDhjvG
    }
}

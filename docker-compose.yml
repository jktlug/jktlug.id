version: "3.8"

services:
    jktlug:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: jktlug-website
        ports:
            - "80:80"
        restart: unless-stopped
        # If you run behind a reverse proxy (e.g. Traefik, Caddy, or another
        # nginx on the host), you can expose only internally and let the proxy
        # handle TLS / jktlug.id:
        # ports: []
        # networks:
        #   - proxy

# networks:
#   proxy:
#     external: true
